{
  "configurations": [
    {
      "name": "Run Docker Compose",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/.dummy.py",
      "preLaunchTask": "docker-compose-up"
    }
  ]
}